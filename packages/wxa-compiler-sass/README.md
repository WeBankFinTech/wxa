# wxa-compiler-sass
wxa sass compiler

# Install 
run `npm` to install. example: `npm i -D @wxa/compiler-sass`

# Usage
```javascript
// wxa.config.js
module.exports = {
    use: ['sass'],
    compiler: {
        sass: {
            // enter your sass option here
        }
    }
}
```

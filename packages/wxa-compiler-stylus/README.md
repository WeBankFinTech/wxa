# wxa-compiler-stylus
wxa stylus compiler

# Install 
run `npm` to install. example: `npm i -D @wxa/compiler-stylus`

# Usage
```javascript
// wxa.config.js
module.exports = {
    use: ['stylus'],
    compiler: {
        stylus: {
            // enter your option here
        }
    }
}
```
